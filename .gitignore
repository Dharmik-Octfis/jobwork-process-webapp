# Dependencies
node_modules/

# Build output
dist/
build/

# Environment files — NEVER commit secrets
.env
.env.*
!.env.example
!.env.production.example

# Generated by scripts/build-app-config.mjs from backend/.env.production.
# Carries real secrets in `env_variables`, so it must stay out of git.
# The committed template is backend/app-config.base.json.
backend/app-config.json

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
*.local

# Test coverage
coverage/