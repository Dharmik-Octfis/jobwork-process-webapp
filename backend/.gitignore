node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# pg_dump output from `npm run db:apply` / `db:backup` — real customer data,
# and large. Keep it on the machine that made it.
/backups

/public
