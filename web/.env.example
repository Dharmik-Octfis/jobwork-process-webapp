# Base URL for the Express API.
# Dev: leave as /api (Vite proxies it to the local backend — see vite.config.ts).
# Prod: set to the deployed API base URL (rebuild required — docs/FRONTEND_SETUP.md §7).
VITE_API_URL=/api
